export interface DefaultState {
    toastMessage: (message: string, severity?: AlertProps['severity'], duration?: number)
}

export interface DefaultState {
    toastMessage: (message: String, severity?: AlertProps['severity'], duration?: number)
}